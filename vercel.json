{
  "buildCommand": "cd nails && npm install && npm run build",
  "outputDirectory": "nails/dist",
  "cleanUrls": true,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "index.html"
    }
  ]
}
